using System;

namespace Course
{
    class Program
    {
        static void Main(string[] agrs)
        {

            //temos o > maior que, < menor que,  >= maior ou igual, <= menor ou igual, == igua e != diferente
            //a principio eles funcionam igual ao JavaScript
            Console.WriteLine(123 > 50);


            //OPERADORES LOGICOS

            //temos também os operadores lógicos, && para que todos sejam true, || para que apenas um é necessário ser true e ! para diferente//

            // && é para quando queremos que mais de uma expressão seja true
            // || é para quando queremos que pelomenos um dos valores seja true
            // ! serve para negação, serve para inversão de true ou false ou para diferente
        }
    }
}